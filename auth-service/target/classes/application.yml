server:
  port: 8083

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5435/auth_db
    username: postgres
    password: postgres

  jpa:
    show-sql: true

  spring:
    flyway:
      enabled: true
